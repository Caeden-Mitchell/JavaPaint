package main;

import main.canvas.CanvasFrame;

/**
 * This is the main class,
 * which is where everything is actually run.
 * Also, this is what the jar is running.
 */
public class JavaPaint {
    public static void main(String[] args) {new CanvasFrame();
    }
}
